<a href="/"><button>Home</button></a> <a href="/purchase/{{ticket._id}}" id="checkout"><button>Checkout</button></a> <a
    href="/profile"><button>Profile</button></a> <a href="/current"><button>Current Session</button></a> <a
    href="/logout"><button>Logout</button></a>
<div>
    <h1>Check out🛒</h1>
    <p><strong>Email:</strong> {{user.email}}</p>
    <p><strong>Cart:</strong> {{user.cart}}</p>
    {{#each tickets}}
    <div>
        <p><strong>Order:</strong> {{order}}</p>
        <p><strong>Date:</strong> {{date}}</p>
        <p><strong>Total:</strong> ${{total}}</p>
    </div>
    {{/each}}
</div>